<view wx:if="{{loading}}" class="empty">登录中…</view>
<view wx:elif="{{error}}" class="empty">{{error}}</view>
<web-view wx:elif="{{url}}" src="{{url}}"></web-view>
<view wx:else class="empty">无法加载 H5。请检查 miniprogram/config.js 的 h5BaseUrl（须 https）及微信公众平台的业务域名配置。</view>
