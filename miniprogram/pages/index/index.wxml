<view class="wrap">
  <view class="hero">
    <text class="title">汤谱</text>
    <text class="sub">本小程序首阶段通过 WebView 打开已部署的 H5 对局页；原生页面与登录可后续迭代。</text>
  </view>
  <button class="btn" type="default" bindtap="onOpenGame" disabled="{{!canOpen}}">进入对局</button>
  <view wx:if="{{!canOpen}}" class="tip">请先在 miniprogram/config.js 中填写有效的 https 地址（非占位符）。</view>
  <view class="hint">微信开发者工具 → 详情 → 本地设置：开发阶段可勾选「不校验合法域名」。</view>
</view>
